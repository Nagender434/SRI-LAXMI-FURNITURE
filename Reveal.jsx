
Action: create_file({"file_text":"import { motion } from \"framer-motion\";\n\nconst easing = [0.16, 1, 0.3, 1];\n\n// Generic scroll reveal wrapper\nexport const Reveal = ({ children, delay = 0, y = 40, className = \"\", ...props }) => (\n  <motion.div\n    initial={{ opacity: 0, y }}\n    whileInView={{ opacity: 1, y: 0 }}\n    viewport={{ once: true, margin: \"-80px\" }}\n    transition={{ duration: 0.9, delay, ease: easing }}\n    className={className}\n    {...props}\n  >\n    {children}\n  </motion.div>\n);\n\n// Masked line-by-line reveal for headings.\n// `lines` is an array of strings.\nexport const MaskedLines = ({ lines, className = \"\", delay = 0, stagger = 0.12 }) => (\n  <span className={className}>\n    {lines.map((line, i) => (\n      <span key={i} className=\"block overflow-hidden\">\n        <motion.span\n          className=\"block\"\n          initial={{ y: \"110%\" }}\n          animate={{ y: \"0%\" }}\n          transition={{ duration: 1, delay: delay + i * stagger, ease: easing }}\n        >\n          {line}\n        </motion.span>\n      </span>\n    ))}\n  </span>\n);\n\n// Same as MaskedLines but triggers on scroll into view\nexport const MaskedLinesInView = ({ lines, className = \"\", stagger = 0.1 }) => (\n  <span className={className}>\n    {lines.map((line, i) => (\n      <span key={i} className=\"block overflow-hidden\">\n        <motion.span\n          className=\"block\"\n          initial={{ y: \"110%\" }}\n          whileInView={{ y: \"0%\" }}\n          viewport={{ once: true, margin: \"-60px\" }}\n          transition={{ duration: 0.9, delay: i * stagger, ease: easing }}\n        >\n          {line}\n        </motion.span>\n      </span>\n    ))}\n  </span>\n);\n","path":"/app/frontend/src/components/site/Reveal.jsx"})
Observation: Create successful: /app/frontend/src/components/site/Reveal.jsx
